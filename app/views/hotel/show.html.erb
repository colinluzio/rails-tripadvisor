  <h2><%= @hotel.name %></h2>

   
   <p><%= @hotel.description %></p>

   
    <div class="cards row">
      <% @comments.each do |comment| %>
      <div class="card col-xs-4">
      
        <h2><%= comment.name %></h2>
        <p><%= comment.description %></p>
        <p><%= @author.name %></p>
      </div>
      <% end %>
    </div>

  </div>
