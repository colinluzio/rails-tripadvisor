<%= debug session %>
<div class="container">

      <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-xs-12 col-sm-8">
			<div class="main">
			
				<%= form_for @hotel, remote: true, :html=>{:multipart=>true} do |f| %> 
   <div class="field"> 
    <%= f.label :title %><br> 
    <%= f.text_area :name %> 
  </div>  
  <div class="field"> 
    <%= f.label :message %><br> 
    <%= f.text_area :description %> 
  </div>  
   <div class="field"> 
    <%= f.file_field :image, multiple: true, name: "hotel[image]" %>
  </div> 
  <div class="field"> 
    <%= f.label :postcode %><br> 
    <%= f.text_area :postcode %> 
  </div>
  <%= f.collection_select :country_id, @countries, :id, :name, include_blank: true %>
  <%= f.hidden_field :owner_id,  :value =>1 %>
  
  <div class="actions"> 
    <%= f.submit "Create", :class => 'submit_button' %> 
  </div> 
<% end %>

<%= form_for @photo, remote: true, :html=>{:multipart=>true}, :url => url_for(:controller => 'photos', :action => 'create') do |f| %> 
   <div class="field"> 
    <%= f.file_field :image, multiple: true, name: "photo[image]" %>
  </div> 
<% end %>
<script id="template-upload" type="text/x-tmpl">
<div class="upload">
{%=o.name%}
<div class="progress"><div class="bar" style="width: 0%"></div></div>
</div>
</script>
<div id="hotels">
</div>
		</div>
  </div>
    <div class="col-xs-6 col-sm-4 sidebar-offcanvas" id="sidebar">
          <div class="list-group">
            <a href="#" class="list-group-item active">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
          </div>
        </div><!--/.sidebar-offcanvas-->
  </div>
  
  </div>
</div>

